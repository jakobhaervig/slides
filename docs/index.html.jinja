<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />

        {% if favicon %}
            <link rel="icon" href="{{ favicon }}">
        {% endif %}

        {% if theme %}
            <link rel="stylesheet" href="{{ theme }}" />
        {% endif %}

        <title>{{ title }}</title>
        <style>
            table {
                border-collapse: collapse;
            }

            th, td {
                border: 1px solid lightgrey;
                padding: 0.5em 1em 0.5em 1em;
            }
        </style>
    </head>
    <body>
        <h1>{{ title }}</h1>

        <table>
            <thead>
                <tr>
                    <th>Slidedeck title</th>
                    <th colspan="3">Links</th>
                </tr>
            </thead>
            <tbody>
            {%+ for s in slideshows %}
                <tr>
                    <td>{{ s.title }}</td>
                    <td>
                        <a href="{{ s.location }}" target="_blank" title="View slides">Slides</a>
                    </td>
                    <td>
                        <a href="{{ s.location }}?showNotes=true" target="_blank" title="View slides with speaker notes">Slides with notes</a>
                    </td>
                    <td>
                        <a href="{{ s.location }}?print-pdf&showNotes=separate-page" target="_blank" title="Download as PDF with speaker notes for printing">PDF for printing</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>


    </body>

        <p>
            Slides last build at <code>{{ build_datetime.strftime('%Y-%m-%d %H:%M:%S') }}</code>.<br />
        </p>
</html>
